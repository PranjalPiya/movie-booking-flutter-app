import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color.fromRGBO(229, 115, 115, 1);
  static const secondaryColor = Color.fromRGBO(229, 115, 115, 1);

  static const whiteColor = Color.fromRGBO(255, 255, 255, 1);
  static const blackColor = Colors.black;
}
